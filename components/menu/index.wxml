<view class="list">
  <view class="list-title">规格</view>
  <view class="list-menu" wx:for="{{list}}">
    <view data-id="{{item.id}}" bindtap="changetype" class="item {{item.id===activeid?'active':''}}">{{item.name}}</view>
    
  </view>
</view>
